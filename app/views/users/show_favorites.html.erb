<tbody>
  <% @products.each do |product| %>
    <tr>
      <td><%= product.name %></td>
      <td><%= product.brand.name %></td>
      <td><%= link_to 'Show', product %></td>
    </tr>
  <% end %>
</tbody>
